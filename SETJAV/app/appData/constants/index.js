import * as modalDialog from './modalDialog';
import * as system from './system';

export const NAME = 'applicationData';

export const MODAL_DIALOG = [NAME, modalDialog.NAME];
export const SYSTEM = [NAME, system.NAME];
